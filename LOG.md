# LOG

Backward-looking. Newest blocks on top. See `ROADMAP.md` for what's
ahead, `SPEC.md` for the full project spec. Process docs at
`@~/.claude/cc-process.md`.

## 2026-05-02 — M5 DAVI: scramble pipeline + value network + training loop (2x2) 🟡 in-progress
**Goal:** First ML milestone. Land the 2x2 training stack end-to-end — backward scramble pipeline (ADI-shaped batches with per-state depths + last_faces), value network (MLP, parameterized on `CubeSpec` so 3x3 drops in at M8), DAVI loop (target-network sync, all-children expansion, terminal clamp), and BFS-optimal `V*` evaluator (the 2x2's small state space gives per-state ground truth — strongest possible correctness signal). Acceptance (per SPEC, post-fix): MAE vs `V*` < 1.0; greedy solve rate ≥ 99% on depth ≤ 14 (2x2 QTM diameter — verified empirically by M5 V\* enumerator at 3,674,160 states across depths 0..14).
**Milestone:** M5 ([plan](plans/m5-davi.md))
**Approach:** Single branch `m5-davi`. Six deliverables, atomic commits each: (1) BFS V\* enumerator (`oracle/v_star_2x2.py`, ~3.6M states cached to `data/v_star_2x2.pkl`, CPU bytes-hashing — GPU pack deferred to M6); (2) `apply_all_moves` extension to `cube/env.py` + `training/scrambles.py` with balanced per-depth slicing for ADI batches; (3) value network parametric on `CubeSpec` (input one-hot → 5000 → 1000 → 4×residual(1000→1000) → 1, ~10M params); (4) DAVI loop (target sync, all-children expansion, terminal clamp, MSE) with `DAVIConfig`; (5) `experiments/davi-2x2/` scaffold + 100k-step training run with JSONL log; (6) acceptance eval vs V\* + `results.md` + `intuition.md` (M4 convention). Plan locked in commits the draft architecture (no width tuning at M5 — M7 sweeps), depth curriculum cap of 14 (God's Number + 3 buffer), JSONL+text analyzer (no tensorboard/wandb/matplotlib).
**Next (cold handoff for next-session Claude):** V*-supervised detour archived; tree reset to clean DAVI scaffold (HEAD `162fb60`). DAVI primitives at `src/rubik/training/davi.py` are tested and ready; `experiments/davi-2x2/davi-baseline/` directory is present but parked (top-of-README banner). To kick off DAVI: (1) revive davi-baseline (drop the parked banner, write a config + run.py invocation that uses compute_targets/davi_step/sync_target); (2) pick first-pass hyperparameters by doing the simplest defensible thing (we have no principled view yet): suggest K_max ∈ [10, 20], target_sync_interval ∈ [100, 1000] steps, batch_size 1024, network size from existing ValueNet (unsized — pick something reasonable like (1024, 256) n=1, smaller than the V*-supervised work suggested since DAVI does work the network differently); (3) run, log per-step loss + periodic V* oracle eval (the existing `eval.py` works — just point it at V_θ and the eval_set.npz); (4) watch the wavefront spread from solved outward over training. Acceptance gate per SPEC remains: macro-MAE vs V* < 1.0 + greedy-solve ≥ 99% on depth ≤ 14 (2x2 QTM diameter). The V*-supervised work has been deleted from m5-davi but is preserved on branch `m5-davi-vstar-supervised-archived` if forensic reference is ever needed. **Methodology debt**: `plans/m5-davi-methodology.md` still has the V*-supervised T1a/T1b/T1c framing — stale, rewrite as part of configuring the first DAVI run, or leave it (it's a methodology that was tried and discarded; next-session can decide whether to keep the file as a record or delete it). **Don't be tempted** to add hybrid samplers, exponential loss reweighting, or shallow-pinning to DAVI without a clear data-driven reason — those were V*-supervised solutions to V*-supervised problems.
**In progress:**
- Commit 1 (V\*): BFS V\* enumerator at `oracle/v_star_2x2.py`. Canonicalization via lex-min over 24 cube-rotation perms (generators U·D' and R·L', composed via M2 sticker tables — no oracle dependency). Full BFS yields 3,674,160 states, max depth 14 QTM (35s wall on M4 Max). Discovered SPEC bug: "11 QTM" is actually HTM God's Number; QTM diameter is 14. SPEC.md fixed (M5 + M6 acceptance gates moved to depth ≤ 14). 15 new tests.
- Commit 2 (env+scrambles): `apply_all_moves(states, spec) -> (B, n_moves, n_stickers)` added to `cube/env.py` (single fancy-index against M4-cached perm). `training/scrambles.py` with `generate_adi_batch` using balanced per-depth slicing (B/max_depth states per depth, remainder to early depths, then shuffle). 22 new tests; cross-validated with V\* (every batch row's claimed depth ≥ V\*-optimal depth).
- Commit 3 (network): `model/network.py` with `ValueNet` parametric on `CubeSpec`. 144 (one-hot 24×6) → Linear(h1) + BN + ReLU → Linear(h2) + BN + ReLU → n × residual(h2,h2) → Linear(1). `body_widths=(h1, h2)` and `n_residual_blocks=n` are required kwargs (no committed defaults — pick by experiment). 10 new tests.
- SPEC.md fix (separate commit f31658f): M5 acceptance gate moved from "depth ≤ 11 (HTM)" to "depth ≤ 14 (QTM, full state-space coverage)". M6 follow-suit. Wording explicitly notes 11 is HTM and 14 is QTM diameter.
- Commit 4 (DAVI loop): `training/davi.py` with `compute_targets`, `davi_step`, `sync_target`. Bellman target via `apply_all_moves`-expanded children + `V_target.forward(no_grad)` + solved-clamp-to-0 + `min_a (1 + V_target)`. `training/config.py` with `DAVIConfig` (frozen dataclass + YAML round-trip via pyyaml; every field required, no defaults). 11 new tests including: target == 1 when V_target zeroed (boundary check); target ≤ 1 with random init (the looser version); buffers (BN running stats) sync correctly (caught by explicit test, would have been a sync-target footgun); davi_step reduces loss over 20 iterations on a fixed batch. Full suite 204 passes in 2.81s.
- **Methodology meta-plan landed (2026-05-02, this session):** wrote `plans/m5-davi-methodology.md` — the detailed 5-tier hand-curated experimentation methodology that the prior pivots were sketching. Spells out per-tier questions, methods, success criteria, output artifacts, and the per-experiment dir template (`configs/`, `run.sh`, `analyze.py`, `intuition.md`, `results.md`). Resolves the chicken-and-egg between LR and network sizing: T1 fits V* via supervised regression (capacity floor decoupled from DAVI dynamics) so any T2+ training failure is unambiguously a dynamics issue, not a capacity issue. V* used as measuring instrument only — never enters the DAVI gradient — so the production training path stays identical to the 3x3 transfer plan. Pre-tier deliverable: `experiments/davi-2x2/eval_set.npz` (100 canonical states × 14 depths = 1400 fixed eval points, V*-labeled, frozen across all tiers for cross-experiment comparability). Original draft included a "user reviews between tiers" seam; user dropped it this same session in favor of deterministic pickers + autonomous overnight orchestration (see Next).
- **Pivot for commit 5 (2026-05-02 chat).** Original plan: "scaffold `experiments/davi-2x2/` + a single 100k-step training run." User redirected: do a Karpathy-style auto-research loop — LLM agent iterates *hypothesis → time-budgeted experiment → analyze → next-hypothesis*, exploring smarter than a formulaic sweep. Differs from M7 (which is *systematic* hparam characterization) — M5's loop is *exploratory*: find ANY config meeting acceptance, log the exploration trace as the second deliverable.
- **Second pivot for commit 5 (2026-05-02 chat, same day).** Pulled back from the Karpathy auto-research framing after follow-up discussion. Three concerns surfaced: (a) Karpathy's loop is a *greedy hill-climber* (each accepted commit becomes the new baseline so changes compound) — for hparam attribution we want each experiment to be *independent* (clean baseline + one config tweak); (b) the original "single 100k-step run" was uncomfortable because the hyperparameters would be borrowed rather than earned — that's not a defensible story for *why* these hparams; (c) 2–3 hour first experiments are too long, but auto-search frameworks (Karpathy's loop, W&B Sweeps + Hyperband) have a real **proxy-gap risk** on cube DAVI specifically: early-training MAE may not predict late-training MAE for configs with LR warmup/decay schedules, and Hyperband would prune exactly the configs we'd most want to characterize. New shape: tiered hand-curated experimentation that mirrors the M4 experiment-loop pattern. Tier 0 measures step time; tier 1 verifies DAVI converges on a downscaled problem; tier 2 sweeps single axes at mid-scale to identify working regimes; tier 3 is the champion full-scale run informed by tier 2. The repo carries no starting reference config — every hyperparameter is earned from these experiments.
- **Decision (W&B for logging only — and deferred).** Considered W&B end-to-end (logging + Sweeps with Bayesian/Hyperband). Settled on **Plan B**: W&B for *logging + visualization only*, not sweep orchestration. Rationale: sweep orchestration bypasses the intuition formation that the M4 `intuition.md` convention is built around; Hyperband prunes runs that look bad early which collides with the cube DAVI proxy-gap risk; the auto-search framing is overkill until M7 (where systematic hparam characterization is the actual milestone goal). Further deferred: W&B-for-logging *itself*, until the tier 1 → tier 2 boundary when ~10 runs accumulate to compare. Until then JSONL + small `analyze.py` is sufficient. To make the eventual flip cheap, tier 0 onward uses a `MetricLogger` wrapper with the W&B passthrough stubbed but disabled — adopting W&B becomes a one-flag flip + `uv add wandb` (~30 min). Logged in ROADMAP backlog. Stack-purity note: this *will* eventually reverse the bootstrap decision to drop tensorboard/wandb/matplotlib — make consciously, when payoff is real, not by default. MPS gotcha worth pre-noting: W&B's GPU telemetry tab is empty on Apple Silicon (NVIDIA-only), so macmon stays our GPU-utilization source.
- **Commit 5 scaffold (this commit).** `experiments/davi-2x2/` landed: (a) `calibrate_step_time.py` — tier 0 sweeps `(batch_size ∈ {256, 1000, 4000}, body_widths ∈ {(256,64), (1000,256), (5000,1000)}, n_residual_blocks ∈ {2, 4})` = 18 cells, each warmup=5 + trials=20 under `time_op` bracket-sync, logs median ms/step + 95% bootstrap CI + per-trial samples + n_params per cell to JSONL. Timed inner loop **includes batch generation + device transfer** so the number reflects what real training pays per step. (b) `run.py` — DAVI training entry point used from tier 1 onward; `--config <yaml> --out-dir <...>`, snapshots resolved `DAVIConfig` to `out_dir/config.yaml` for reproducibility-from-output-alone, runs the loop, periodic target sync + checkpoint, logs `(step, loss, step_seconds)` events via `MetricLogger`. (c) `MetricLogger` at `src/rubik/training/metric_logger.py` — append-only JSONL with flush-per-record, optional duck-typed `wandb_run` passthrough (Protocol-typed, no `wandb` import at module load — keeps the dep truly optional and trivial to mock in tests). (d) `results.md` skeleton with the four-tier methodology table + per-tier status placeholders + Intuition section reservation. (e) `DAVIConfig` defaults stripped — every field required, missing-key in YAML raises `TypeError`, replaces `test_davi_config_defaults_match_spec` with `test_davi_config_requires_all_fields_explicit` + adds `test_davi_config_from_yaml_missing_field_raises`. 11 new tests (10 MetricLogger + 1 DAVIConfig from_yaml missing-field); full suite 215 passes; ruff clean.
- **Cleanup commit (2026-05-03 chat, post-explorations).** First pass at tier 0 / tier 1 / tier 2 experimentation drifted off methodology — leaned on borrowed reference values rather than earning them, ran sweeps at the wrong baselines, generated noise dressed up as findings. Reset `m5-davi` to commit 5 (this scaffold); archived the discarded explorations on branch `m5-davi-explorations-archived` (commit 68ecf81) for future reference if needed. Then audited the codebase for any remaining "cheat" references — specific values borrowed from prior published work that act as defaults or anchors. Removed: `experiments/davi-2x2/baselines/deepcubea_defaults.yaml` (file + dir), `plans/archive/llm-draft-spec.md` (file + dir, since it carried specific architecture/hparam tables). Edited: `src/rubik/model/network.py` docstring (drop specific borrowed body shape, keep parametric framing) + replaced the borrowed-shape constructor defaults with tiny test-only placeholder values `(64, 32) / 1` clearly labeled as such; `src/rubik/training/config.py` docstring (rewrote rationale without naming the deleted yaml); `experiments/davi-2x2/results.md` (dropped reference-config mention); `plans/m5-davi.md` (rewrote network-width section + commit 4 description); `tests/model/test_network.py` (deleted `test_param_count_in_expected_range` which encoded the borrowed shape as an invariant; updated `test_body_widths_parameterizable` to compare against placeholder, not borrowed-default); `CLAUDE.md` (removed pointer to the deleted draft-spec). 215 tests pass. Going forward: hyperparameter values are an empirical question; pick them by experiment, not by inheritance.
- **Overnight orchestrator dry-run + reset (2026-05-03 chat, this session).** Built D1–D8 scaffolding (eval_set, eval, t1–t5 dirs, pickers, orchestrate) and ran the full T1→T5 autonomous chain (167 min wall, exit 0). Chain itself worked; substance was bad. **T1 lattice (`h1 ∈ {32, 64, 128, 256}` × `h2 ∈ {32, 64, 128}`) was systematically below the input dim (144) — the entire grid sat in the under-capacity regime, every cell plateaued at val_mae ≈ 0.89 regardless of param count.** T2–T5 inherited that floor and produced numbers that don't measure what they claim (LR live zone on an underfitting net, dynamics surface on a non-existent baseline, kill rule on degenerate labels, 100k acceptance run on a compromised chain). **Reset commit (this entry)**: archived overnight HEAD (`f4c128c`) to branch `m5-davi-overnight-broken-t1` for forensic queryability; hard-reset `m5-davi` to `9cbfe63` (methodology + cold handoff, pre-scaffolding); deleted untracked tier directories (configs preserved on archive branch; gitignored `runs/` JSONL data lost — accepted per the throwaway-branch framing). **Methodology fix in this commit**: rewrote `plans/m5-davi-methodology.md` §T1 from a single 6-cell `(width, residual)` bundled grid to a two-phase plan — Phase A walks down widths at `n_residual_blocks=0` (no residuals; bracket-from-above starting at `(8192, 2048)`), Phase B sweeps `n_residual_blocks ∈ {0, 1, 2, 4}` only at the chosen `(h1*, h2*)`. Rule learned: bracket capacity from *above* (start with should-clearly-succeed, walk down) rather than *below* (start small, hope success kicks in) — the latter can't tell whether the floor was found or never sampled. Also: residual blocks are weak optionality at our network depth (7–11 layers), not necessity; n=4 was anchoring on DeepCubeA and shouldn't have been in the prior grid. Going forward: T1 only, no chain to T2 until T1 is committed and reviewed; the overnight-orchestration pattern is wrong shape for this work.
- **T1 Phase A halt-and-debug (2026-05-04 chat, this session).** Ran the corrected two-phase T1 manually-paced. Built `experiments/davi-2x2/t1-capacity/{supervised.py, configs/phaseA_*.yaml, run.sh, analyze.py, README.md, results.md, intuition.md, _picks.json, plots/phaseA_pareto.{svg,html}}`. **Pre-flight T0 extrapolation:** `(8192, 2048)` n=0 batch 1024 projected to ~50–70 ms/step (over the ~30 ms ceiling) → **dropped top cell, re-anchored at `(4096, 1024)` per cold handoff escape clause**, 5 cells instead of 6. **Phase A result: all 5 cells failed.** Across a 23× param scale-up (207K → 4.80M), final val_mae varied by 0.006 (range 0.8979–0.9037), all sitting 0.03 above the predict-the-mean baseline of 0.9286. The Pareto frontier is essentially horizontal — `n_params` explains zero. **Three diagnostic variants** then probed the axes Phase A held fixed: (a) `(4096,1024)` n=0 30k steps LR 3e-3 → 0.8456; (b) `(1024,512)` n=2 7k steps LR 1e-3 → 0.8652; (c) `(2048,512)` n=4 30k steps LR 3e-3 → 0.7968. Each axis helps a little; combining all three helps most. None get close to 0.5. **Diagnosis (high-confidence H1):** MSE loss × peaked V\* depth distribution (83% of states at depths 10–12, 0.02% at 0–4) × BN-everywhere MLP creates a mean-collapse trap — predictions order correctly by depth (depth 6 → 7.0, depth 11 → 10.9, depth 13 → 11.3) but are *systematically compressed toward the mean* (pred std 0.79 vs target std 1.16). Train-mode and eval-mode preds differ by < 0.02 → BN running stats aren't drifted; compression is in body representation magnitude, not BN drift. **Per methodology §T1: Phase B not opened** — failure isn't capacity-shaped, it's loss-distribution-shaped, and capacity scaling can't see that bug from any angle. **Next-cycle work** (in `intuition.md` Open questions): single-cell debug experiments — (1) L1 loss instead of MSE, (2) drop BN-before-head ablation, (3) LR warmup schedule, (4) memorize-1k-state sanity check. Also a methodology-level question: should T1 become a two-question tier where loss/normalization choice is asked *before* capacity? T2 stays closed until T1 has a real `(h1*, h2*, n*)` pick.
- **T1 debug: memorize-1k sanity check (2026-05-04 chat, this session).** Built `experiments/davi-2x2/t1-capacity/memorize_1k.py` + `configs/memorize_1k_2048x512.yaml` adapted from `supervised.py`. Trains on a fixed depth-stratified 1000-state subset (indices saved to `out_dir/subset_idx.npy` for reproducibility), full-batch=1000, MSE loss, lr=1e-3, 10k steps, on `(2048, 512)` n=2 (~2.4M params — far more than enough for 1k targets). **Pass criterion train MSE ≤ 1e-3 hit at step 63**; train MSE dropped to ~1.2e-9 by step 10000. Courtesy val MAE on a 2000-state held-out slice stayed at ~1.0 throughout — exactly the expected memorize-without-generalize signature. **Verdict: machinery clean.** Did not need lr=3e-3 / lr=1e-2 / `(4096, 1024)` fallback cells. Phase A's plateau is genuine MSE × peaked-V\*-distribution × BN-everywhere interaction, not a training-loop or data-path bug. H1 (mean-collapse trap from loss-distribution shape) still uncontested. Test count: 214 (one drop from 215 — pre-existing, not from this work). Next: run L1-loss experiment on full V\* corpus to test the loss-formulation half of H1. Commit b262f6e.
- **T1 debug: L1-loss diagnostic (2026-05-04 chat, this session).** Extended `supervised.py` and `memorize_1k.py` to take a required `loss: "mse" | "l1"` field; backfilled `loss: mse` into all 5 phaseA configs + memorize_1k config (no defaults policy). Added eval-time logging of `pred_mean` / `pred_std` so the compression signature can be tracked per run. Two new cells: `l1_2048x512_n0.yaml` (apples-to-apples vs `phaseA_3`) and `l1_2048x512_n2.yaml` (cold-handoff cell). **Headline: H1's loss half does NOT hold.** L1 collapses to its predict-the-median floor (0.8583) instead of MSE's predict-the-mean floor (0.9286) — both losses collapse, just to different constants. Final val_mae: L1 n=0 = 0.8819 (0.024 *above* its median floor); L1 n=2 = 0.8667; vs MSE n=0 phaseA_3 = 0.8979 (0.03 *below* its mean floor — MSE actually has a touch more learned structure than L1 at this budget). **Unexpected twist:** L1 compressed *harder* than MSE — pred std 0.19–0.25 under L1 vs ~0.79 under MSE — because L1's sign-only gradient goes to zero near the median (above-median and below-median grads cancel), killing the tail-pull. MSE's magnitude-scaled gradient was actually pulling toward the tails more, just not enough to escape the bulk basin. **Strictly:** experiment varied loss only, so falsifies "loss formulation is the key lever," not the wider H1 (loss × distribution × BN). Compression is loss-symmetric → architectural side (BN clamping body activations to unit variance) is now the leading suspect. Residual help (n=2 vs n=0) was -0.015 abs, similar magnitude to MSE-side residual help — loss formulation does not dominate residual contribution. **Methodology update:** with loss out of the picture, the "T1 reframed as loss/normalization-first tier" question collapses to *normalization-first, capacity-second* — cleaner two-question tier. Next: matched-budget BN ablation on MSE n=0 — cell A (BN intact, baseline with pred_std logged at 7k budget — current 0.79 figure was from a 30k-step diagnostic), cell B (BN dropped). If pred_std climbs toward 1.16 and val_mae drops below 0.7, H2 (BN-everywhere amplifies the trap) confirmed. Test count 214, ruff clean. Commit 365e645.
- **T1 debug: BN ablation + autonomous investigation (2026-05-04 chat, this session) — sampling-was-the-trap.** User authorized ~60 min autonomous investigation with bigger networks (`(4096, 1024)`) and BN ablation as primary axes. Built a clean `normalization: "bn" | "none" | "ln"` toggle on `ValueNet` (required kwarg, no default; LayerNorm via `nn.LayerNorm(width)`, none via `nn.Identity()`). Backfilled 8 existing configs with `normalization: bn`. Added 6 normalization tests. Ran 8 cells at 7k-step matched budget on MSE: BN/no-BN × `(2048, 512)` n=0 / `(4096, 1024)` n=2, plus an lr=3e-3 variant on the BN big cell, plus three depth-balanced (`dbal`) sampler cells. **Headline: the val_mae ~0.90 plateau is a sampling problem, not BN/loss/capacity.** Just switching uniform-over-V\* sampling to depth-balanced (sample equally per depth bucket) drove pred_std from 0.35 → 1.15 (target std 1.16) on the same network at the same budget. Per-depth prediction means under uniform: depth 3 → 5.69, depth 13 → 10.78 (collapsed to ~10.7 mode); under depth-balanced: depth 3 → 4.06, depth 13 → 11.32 (tracks truth). **BN is innocent — slightly *helpful* even**: BN+dbal hit pred_std 1.15 vs no-BN+dbal at 1.07. **The val_mae paradox:** depth-balanced training gave *higher* uniform val_mae (1.13 vs 0.90) because the val set is also uniformly sampled from V\* and inherits the same bulk-heavy depth distribution, so predict-the-mean (val_mae ≈ 0.93) is near-optimal on a bulk-weighted MAE — the eval metric was rewarding the bulk-collapse strategy. **Methodology reframe:** T1 splits into T1a (sampling — answered yes), T1b (eval metric — need macro-MAE = uniform-per-depth, so dashboard can't be gamed), T1c (capacity floor under fixed T1a+T1b). The widths-then-residuals two-phase plan still applies but moves *inside* T1c. The prior "loss/normalization-first reframe" question collapses — both axes were wrong; lever was data-side. **Caveat to verify:** depth-balanced sampler currently samples with-replacement from per-depth buckets, and depth 0 has 1 state, depth 1 has 9 states. Tail buckets give a single state's gradient repeated per batch — likely tail-bucket gradient noise. Whether this matters at 30k steps is the next test. **Suggested next experiment** (per agent's writeup): 30k-step `dbal_4096x1024_n2` with macro-MAE added to eval logger. H4 predicts macro-MAE drops below 0.5 while uniform val_mae stays near 1.0. If macro-MAE doesn't drop, sampling is necessary-but-not-sufficient and tail-noise / something deeper is in play. Test count 214 → 220 (6 new). 4 commits: 6337396 (toggle infra), c9dcce9 (primary 4 configs), 301e429 (sampler + follow-up), 72ce969 (writeup).
- **T1c verification cycle (2026-05-04, this session) — H4 falsified at this sampler.** Methodology doc rewritten to lock in the T1a/T1b/T1c split — `plans/m5-davi-methodology.md` §T1 replaced top-level "capacity floor (supervised V* regression)" framing with the three-question structure, with Phase A/B widths-then-residuals nested inside T1c (sampler/normalization/loss now closed-form choices, not axes). The prior framing's text isn't preserved verbatim in the doc — the journey lives in `t1-capacity/intuition.md` (single-source-of-truth: methodology = forward-looking instruction, intuition = historical record). Code change for T1b: `_eval_val_mae` in `supervised.py` renamed `_eval_val` and now returns a metrics dict — adds `macro_mae` and `per_depth_mae` (dict[int, float]). Uniform val_mae retained for backward comparability. JSONL `event:"eval"` schema gains both fields; `run_end` event also gains `final_macro_mae`. `memorize_1k.py`'s local `_eval_val_mae` left untouched. Launched 30k `dbal_4096x1024_n2_30k.yaml` (only diff: `n_steps: 30000`) — H4 prediction was macro_mae < 0.5 by step 30k. **Result: H4 FALSIFIED.** macro_mae plateaus 1.05–1.17 from step 7k → 30k with no descent (final 1.17 *higher* than 7k value 1.13). Train loss falls 1.71 → 0.31 over 30k steps. Smoking gun is per-depth: depth 3 MAE goes 0.43 (step 1k) → 1.06 (7k) → 1.40 (15k) → **2.04** (30k) — model gets *worse* at depth 3 over training. Depth 3 has 102 train states; depth-balanced sampler draws ~68 with-replacement samples per batch from those 102 → ~2M gradient updates against 102 unique states by step 30k → pure tail-bucket overfit. Modal depth 11 (1.08M train states) keeps improving (0.92 → 0.70). New leading hypothesis **H5: with-replacement-from-bucket causes tail overfit** (high confidence — train/val gap mechanism is direct). T1c's pre-requisite gate fails; widths-then-residuals Phase A/B sweep is *not* the right next move. Methodology debt also surfaced: val eval set (10k random states from 20% of V\*) contains only depths 3–13 — depths 0, 1, 2, 14 too rare to land in the random sample. macro-MAE is averaged over 11 depths, not 15. Pre-tier deliverable `eval_set.npz` (depth-stratified) was specified in methodology but never landed; this run is the forcing function. Writeup in `t1-capacity/intuition.md` §"T1c verification — 30k dbal_4096x1024_n2". **Proposed next experiment (single-question per "T1 is yes/no"):** **frequency-weighted MSE under uniform sampling** — same cell, sample uniformly (every train state seen at natural rate, no artificial repetition), weight each sample's loss by `w(d) = N / (n_buckets · freq[d])` so per-depth gradient contribution equalizes in expectation but tail buckets contribute via the *full* set of unique tail states each batch instead of with-replacement copies. If macro_mae descends below 0.5 (or even just below 1.0), H5 confirmed and T1c's widths sweep can proceed under the new sampler. Subsidiary cheap experiment: re-evaluate existing 7k cell checkpoints under the new `_eval_val` to add macro-MAE columns retroactively (no retraining; just a checkpoint-reload analyzer script). Both pending user direction. Run dir: `experiments/davi-2x2/runs/20260504T145533Z_dbal_4096x1024_n2_30k/`.
- **Reset to DAVI (2026-05-04, this session) — V\*-supervised detour archived.** After running a hybrid sampler experiment and reviewing results, the user did external research on DAVI and recognized that the entire post-DAVI-primitives trajectory (T1 Phase A, T1 debug cycles, T1c, V\*-weighted scaffolding, this session's hybrid sampler) was a methodological detour: V\*-supervised regression as a "find network capacity first" tier-zero step that swallowed the milestone. M5's actual goal — DAVI loop with backward random scrambles, two-network bootstrap, Bellman target with solved-clamp-to-0, wavefront propagation — has working primitives at `src/rubik/training/davi.py` (commit 4 of M5, tested) but was never run. **User directive**: archive the detour, reset to clean DAVI state, then run DAVI; use V\* purely as oracle for evaluation, never as training signal. **Archived**: branch `m5-davi-vstar-supervised-archived` at HEAD `d75b64e` carries all detour work for forensic queryability. **Reset target**: hard-reset m5-davi to `5fb95a2` (M5 DAVI infra: eval pipeline + depth-stratified eval set; davi-baseline parked). Plus one cleanup commit deleting `experiments/davi-2x2/t1-capacity/` (predated 5fb95a2, was V\*-supervised machinery). **What survives**: DAVI primitives (compute_targets, davi_step, sync_target — buffer-aware target copy catches BN running-stats footgun), DAVIConfig with normalization field, ValueNet (parametric on CubeSpec, with bn/none/ln toggle), eval pipeline at `experiments/davi-2x2/eval.py` (macro-MAE + greedy-solve), depth-stratified `eval_set.npz`, parked `davi-baseline/` directory. **What was lost (intentionally)**: V\*-supervised methodology findings — they applied to a fitting problem (regress fixed V\* labels with MSE) that's structurally different from DAVI (regress Bellman-bootstrap targets recomputed every batch from V_target). The "MSE × peaked depth distribution → mean collapse" trap doesn't directly apply to DAVI because targets aren't fixed depth labels. The "depth_balanced sampling causes tail-overfit" finding doesn't apply because DAVI generates fresh random walks each batch, not bucket-sampling from a fixed corpus. **Methodology debt**: `plans/m5-davi-methodology.md` still carries the T1a/T1b/T1c V\*-supervised framing — stale, will be rewritten when DAVI runs are configured (next session). **Next**: configure first DAVI run. Need to pick K_max (scramble length), target_sync_interval, n_steps, network size — none of which we have a principled view on. Per user directive: try things, find something that works, then optimize. Run, evaluate against V\* oracle, iterate.

## 2026-05-02 — M4 follow-up: env.py CPU-sync cleanups + iteration-loop pattern ✅ done
**Goal:** Patch the two CPU-sync gotchas in `apply_moves` that M4 measured but deferred (bounds check at `env.py:80` forcing 8 scalar extractions per call; perm migration at `env.py:70` re-copying the static perm table on every call). Co-equal goal: establish the **measure → fix → measure → analyze** iteration-loop pattern as a documented, reusable shape — using this fix as the worked example. M4 was the first perf milestone (taught us what to measure); this block is the first perf-fix block (teaches us how to iterate). Future variant-sweeps in M7 inherit the pattern.
**Milestone:** drive-by — closes both deferred follow-ups from M4 ([plan](plans/m4-perf-1.md))
**Approach:** Single branch `env-cpu-sync-cleanups`. Two fixes applied independently so their contributions can be attributed: (1) relocate bounds check to before `move_idxs.to(states.device)` so it runs on whatever device the caller built it on — CPU in the realistic pattern (`random_scrambles` inner loop, future beam-search children expansion); (2) add per-`(spec.name, device)` cache for the device-resident perm so we migrate the 12×24 int64 table once per device, not per call. Each fix gets its own commit + remeasurement against a focused 4-cell mini-bench (B=1, 64, 8192, 2M — spans dispatch-dominated → saturated regimes). Predictions written before measuring; analysis logged after. New helper `experiments/mps-methodology/bench_apply_moves.py` is reusable for future hot-path bench cycles. Methodology results.md gets a "Cleanup validation" section (before/after tables + per-fix attribution) and a new `## Iteration loop pattern` section codifying the pattern. Verifier baseline drops to 0 (becomes a regression detector); `probe_profiler.py` updated to keep `move_idxs` on CPU so the trace reflects the realistic call pattern.
**In progress:**
- Block opened on branch `env-cpu-sync-cleanups`. Roadmap reordered (3x3 consolidates at M8, after 2x2 ships end-to-end through M5/M6/M7).
- Commit 1 (open): bench_apply_moves.py landed (4-cell mini-bench, CPU move_idxs to match production). Baseline captured (B=1: 685 μs, B=64: 620 μs, B=8192: 628 μs, B=2M: 7,771 μs). Predictions written into §7.1: shape "small-B big relative win, large-B small relative win, absolute savings ~0.6–1 ms/call." Verifier confirmed pre-fix baseline of 8 syncs/call.
- Commit 2 (fix #1 + verifier evolution): env.py bounds-check relocated; probe_profiler aligned to CPU `move_idxs`; verifier reframed from name-counting to duration-thresholding (50 μs split). §7.2 logged 33–42% improvement at small B (less than predicted 70–93% — pre-fix sync cost was ~210–260 μs, not 500–700 μs); B=2M *regressed* 4.8% (real, not noise). §7.2.1 documented the verifier evolution as a methodology side-fix.
- Commit 3 (fix #2 + analysis): env.py perm cache + identity test landed. §7.3/§7.4/§8 written. Headline: **fix #2 healed the fix #1 B=2M regression** (4.8% loss → 0.9% loss within noise). The two fixes interact — visible only because we measured between them. Findings #1 and #2 in §4.5 marked ✅ Resolved with date + cross-link.

**Outcome:**
- Acceptance gate green: `142 passed in 1.42s` (1 new test on top of M4's 141 — the perm-cache identity assertion); `ruff check` clean; `ruff format --check` clean. Verifier PASS at 0 GPU sync stalls (events with dur > 50 μs); `_to_copy` events down from 10 to 5 (perm migration eliminated; per-call `move_idxs` migration unchanged).
- Headline numbers: `apply_moves` ~2× faster at small/medium batches (the inner-loop regime that DAVI training and beam search will inhabit). B=1: 685 → 325 μs (2.11×). B=64: 620 → 292 μs (2.12×). B=8192: 628 → 378 μs (1.66×). B=2M: 7,771 → 7,837 μs (no change, within noise).
- 2 source files modified: `src/rubik/cube/env.py` (bounds check moved 4 lines earlier, dtype-conversion preserved; new `_DEVICE_PERM_CACHE` dict + `_perm_for_device` function; `apply_moves` calls the cached lookup). `tests/cube/test_env.py` gets `test_perm_cache_returns_same_tensor_per_device` for `is`-identity regression.
- 2 experiment files modified: `experiments/mps-methodology/probe_profiler.py` (drops `.to(device)` on `move_idxs`, with a comment explaining the production-pattern alignment); `experiments/mps-methodology/verify_no_cpu_sync.py` (rewritten — duration-thresholding instead of name-counting; renamed constants).
- 1 new experiment helper: `experiments/mps-methodology/bench_apply_moves.py` — 4-cell mini-bench at B=1, 64, 8192, 2M, with `--label` for variant studies. Reusable for any future hot-path bench cycle. Output JSON files under `runs/cleanup-loop/` (gitignored).
- Methodology results.md: 3 new sections + 2 status updates. §7 (Cleanup validation) is the first full worked example of the measure→fix→measure→analyze loop in this project. §7.2.1 captures the verifier rewrite as a methodology contribution, not a side note. §8 abstracts the iteration loop pattern for reuse — a deliverable beyond the fix itself, and the second deliverable beyond the original plan in this block (the first being the M4 intuition convention).
- **Decision (recorded):** **Apply changes one at a time when contributions can plausibly be attributed.** This block's most informative finding — fix #2 healed fix #1's B=2M regression — would have been invisible if both fixes had been applied at once. The cost of measuring between fixes was ~30 seconds of bench time per cycle; the value was a finding that recasts the per-fix story from "two independent improvements" to "two interacting fixes that are net-positive only together."
- **Decision (recorded):** **Verifiers should prefer physics-grounded thresholds over name-based heuristics where possible.** The original M4 verifier counted `aten::item` and `aten::_local_scalar_dense` events — a reasonable proxy *for the M2 call pattern*, but the proxy broke when the call pattern changed (CPU `move_idxs` produces the same events at sub-μs cost). Switching to duration-thresholding (50 μs split between sub-μs CPU dispatch and 100s-μs GPU sync stalls) is robust to call-pattern changes. Documented in §7.2.1.
- **Decision (recorded):** **Predictions before measurements, written into the doc.** §7.1 captured the shape hypothesis ("relative savings shrink with B; absolute savings ~0.6–1 ms/call") *before* running fix #1. The fact that the magnitude prediction was falsified is a high-information outcome — it tells us the methodology's pre-fix sync-cost estimate was a per-event ceiling, not a per-call sum. Without the written prediction, this would have looked like "the fix worked, ship it"; with the prediction, it's a small but real model update.
- **Decision (recorded):** **The verifier evolution counts as a methodology contribution worth shipping in this block, not a separate one.** When the cleanup-loop revealed a verifier flaw, fixing the verifier was the right scope — anything else would have shipped a non-functional verifier on `main`. §7.2.1 documents the rewrite reasoning so future verifier changes can refer to it.
- **Decision (recorded):** **bench_apply_moves.py uses CPU `move_idxs` to match production reality**, not MPS-prebuilt as in `batch-sensitivity-2x2/run.py`. The two scripts measure different regimes for the bounds-check sync; the former matches `random_scrambles`'s inner loop (build move tags on CPU, let `apply_moves` migrate). Documented in the script header for any future reader.
- **Deviation (recorded):** Plan said cleanup #1 would save ~0.5–0.7 ms/call. Actual: 210–260 μs. The methodology's "0.5–0.7 ms" figure summed individual `is_nonzero` durations from the trace, but those durations overlap (the second `.any()` waits less than the first because the queue is partially drained). Honest scope correction logged in §7.2 lesson 1.
- **Deviation (recorded):** Plan said B=2M would see 7–13% improvement from fix #1 alone. Actual: 2.4% *regression*. Hypothesis (§7.2 lesson 3): pre-fix bounds-check syncs gave the `move_idxs` migration time to settle before the gather; post-fix the migration runs later in the call. Healed by fix #2 (cached perm leans the dispatch pipeline; net result at B=2M is 0.9% slower than baseline, within noise). The deviation became the headline finding.
- **Deviation (recorded):** Verifier was supposed to drop its `EXPECTED_BOUNDS_CHECK_ITEMS_PER_CALL` constant from 8 to 0 after fix #1. Actual: the constant was renamed `EXPECTED_GPU_SYNCS_PER_CALL` and the underlying detection mechanism switched from name-counting to duration-thresholding because the cleanup-loop revealed CPU-side `__bool__` calls also emit `aten::item` events. Bigger change than planned, justified by §7.2.1.
- **Deviation (recorded):** Plan didn't anticipate the per-call `move_idxs` CPU→MPS migration as a separate cost. Surfaced in §7.2 lesson 2 when probe_profiler was aligned to CPU input — the trace then showed 10 `_to_copy` events for 5 calls (5 perm + 5 move_idxs) where the M4 trace had only shown 5 (because probe_profiler had hidden the move_idxs migration by pre-migrating). Logged as OQ2 — defer pre-migrating in `random_scrambles`/beam search until profiling shows it's a bottleneck.
- **Deviation (recorded):** Plan estimated 30–45 min total wall time. Actual: closer to 90 min, mostly because of the verifier rewrite + analysis writeup. The fixes themselves were ~10 min of actual code; the methodology contributions were the bulk of the work.
- Drive-by: The basedpyright complaints about `Cannot access attribute "X" for class "int"` in `apply_moves` and `valid_next_moves_mask` are pre-existing from M2 (the ternary `Tensor | int` narrowing doesn't carry through pyright's type inference). Out of scope for this block; logged here so a future agent doesn't think the cleanups introduced them.
- Drive-by: `bench_apply_moves.py` had a bug in its first run — `out_path.relative_to(REPO_ROOT)` failed when `--out-dir` was passed as a relative path. Fixed inline by `out_path.resolve()` before `.relative_to()` with a try/except fallback to absolute. The bench output had already been written successfully before the print failed, so no data loss.
**Commits:** 2234740 (open), a792869 (fix #1), 2a84cee (fix #2)

## 2026-05-02 — M4 Perf-1: MPS methodology + batch sensitivity (2x2) ✅ done
**Goal:** Stand up the project's first perf milestone — a reproducible MPS measurement methodology on this M4 Max, a batch-size sensitivity sweep on `apply_moves` and `random_scrambles` with bootstrap 95% CIs, profiler-trace verification of no CPU round-trips in the hot path, and the `experiments/<name>/` pattern itself (template for M8). Co-equal goal: build genuine GPU intuition — where does this hardware saturate, what's parallelizable, where does dispatch overhead end. The writeup is the deliverable, not just an output.
**Milestone:** [plans/m4-perf-1.md](plans/m4-perf-1.md)
**Approach:** Single branch `m4-perf-1`. Three measurement layers that cross-check each other: (1) bracket-sync timing in `src/rubik/perf/bench.py` (`time_op`, `bootstrap_ci`) — primary truth; (2) `macmon` external correlation — answers "are we actually on the GPU?"; (3) `torch.profiler` — investigation tool when (1) and (2) disagree, with explicit fidelity probe upfront since MPS profiler has historically been patchy and we're on torch 2.11 (unknown). Two experiment dirs: `experiments/mps-methodology/` (writeup + probes + correlator) and `experiments/batch-sensitivity-2x2/` (sweep + analysis). Adds `pyyaml` dev dep for config files. M4 measures, doesn't optimize — bounds-check + perm-migration costs get flagged if material, fixing them is a follow-up block.
**In progress:**
- Block opened on branch `m4-perf-1`. Plan committed to `plans/m4-perf-1.md`.
- Commit 4: profiler probe + baseline-aware verifier landed. Profiler trace at `experiments/mps-methodology/runs/<ts>/profiler/trace.json` (gitignored). Verifier PASS at 40 ≤ 40 (baseline = 8 scalar extractions per `apply_moves` call from the bounds check at `env.py:80`, × 5 active iterations).
- Finding (deferred): `apply_moves` bounds check (`env.py:80`) creates host-blocking syncs on every call via the `(t.any() or t.any())` pattern — empirically 8 scalar extractions per call, ~0.5–0.7 ms of pure host-GPU sync overhead. Documented as gotcha #1 in `experiments/mps-methodology/results.md`. Fix tracked for follow-up block.
- Finding (deferred): `_perm_for(spec).to(states.device)` at `env.py:70` migrates the perm table to MPS on **every** call (5 `aten::_to_copy` events for 5 calls). Documented as gotcha #2; fix is a per-(spec, device) cache. Tracked for follow-up.
- Decision: `EXPECTED_BOUNDS_CHECK_ITEMS_PER_CALL = 8`, not 4. The brief predicted 4 (one item + one `_local_scalar_dense` per `__bool__()` × 2 sides); actual on torch 2.11 is 8 because the `aten::lt` / `aten::ge` itself emits a scalar-extract pair before the `.any().__bool__()` does. The verifier is empirical, not predictive — the constant reflects what the trace says.
- Commit 5: macmon probe + correlator landed. Worked example: B=8192 bench-predicted busy=1.000 (workload-bound, 52529 calls in 25.0 s window), macmon-measured 0.952, verdict AGREE (delta 0.048, gpu_power 3.94 W). Pattern: redirect-to-file → one-line digest, never stream.
- Decision: bench-bracket per-call (0.694 ms) inflates over workload-effective per-call (0.476 ms) by ~0.22 ms — that's the `mps.synchronize()` stall on each side of the timed bracket. Naive substitution `n_calls × bench_per_call / window` predicts 1.458 (DIVERGE). Methodology rule (now in section 5): for the busy-fraction comparison, use steady-state pipeline `min(1.0, n_calls × workload_per_call / window)`, not bench-bracket per-call. Bench measures one-shot latency including sync stalls; macmon measures continuous utilization.
- Commit 6: batch-sweep driver + analyzer + steady-state spot-check landed. Smoke-verified (1 cell × 3 trials, B=64). Full sweep + numbered results.md land in commit 7.
- Commit 7: full sweep ran 15/15 cells (8 `apply_moves` B=1..2M + 7 `random_scrambles` B=1..262K, top trimmed via `per_op_batch_sizes` because depth × CPU `move_seqs` is the bottleneck not GPU work). Steady-state spot-check at B=8192: factor 0.866 (warmer cache than commit 5's 0.686). `apply_moves` peak 281 M states/s at B=2M; saturation onset B=4096→32768 (eff 0.79), heavy by B=262144 (eff 0.43). `random_scrambles` peak 556 K/s at B=262K, saturates already at B=64. Analyzer upgraded from raw % gain to **scaling efficiency** = throughput_ratio / batch_ratio (regime-correct for non-uniform 8x batch steps).
- Convention established: every experiment dir's `results.md` ends with a hand-curated `## Intuition` section in epistemically honest format — Observations (mechanical) → Hypotheses (with confidence + evidence + verification plan) → Open questions → caveats on most speculative claims. Hand-written in `intuition.md`; analyzer reads + appends. Goal: capture conceptual learning per experiment without anchoring future agents to overconfident framings. Documented in `CLAUDE.md`.
- Decision: `move_seqs` CPU bottleneck claim for `random_scrambles` demoted from conclusion to **medium-confidence hypothesis** (correlation + code reading, not measurement). Verification plan logged in `intuition.md` H3 — profile `random_scrambles` at B=8192 to confirm. Anti-pattern noted: writing "X is the bottleneck" without a profiler trace is anchoring, not investigation.

**Outcome:**
- Acceptance gate green: `141 passed` (6 new perf tests on top of M3's 135), `ruff check` clean, `ruff format --check` clean. Both methodology + batch-sensitivity `results.md` reproduce from a clean shell via the verification block in `plans/m4-perf-1.md`.
- New module: `src/rubik/perf/{__init__.py, bench.py}` (~80 lines) — `time_op` (bracket-sync timer, raises if MPS requested but unavailable) + `bootstrap_ci` (median + 95% CI, deterministic under seed). Re-exported from `rubik.__init__`. Future training/search code reuses the same primitive instead of reinventing measurement.
- Two new experiment dirs. `experiments/mps-methodology/` (probe_profiler.py, verify_no_cpu_sync.py, probe_macmon.sh, _macmon_workload.py, correlate_macmon.py, results.md, intuition material in `results.md` itself). `experiments/batch-sensitivity-2x2/` (config.yaml, run.py with `per_op_batch_sizes` support, run_steady_state.py, analyze.py, results.md, intuition.md). `pyyaml` added as dev dep.
- Numbers (this M4 Max, torch 2.11): `apply_moves` peaks at **281 M states/s at B=2M** (~325 M/s pipelined after ×1/0.866 correction); saturation onset between B=4096 and B=32768 (efficiency 0.79); heavy saturation by B=262144 (eff 0.43). `random_scrambles` peaks at **556 K scrambles/s at B=262K**; saturates already at B=64. macmon validated 95.2% GPU busy at B=8192 sustained, AGREE verdict against bench-predicted busy.
- **Decision (recorded):** Triangulation strategy — bench (primary truth, the only reliable wall-time on MPS), macmon (engagement / "are we on the GPU?"), torch.profiler (investigation when 1 + 2 disagree, dispatch-only on torch 2.11). Apple Instruments / Metal System Trace flagged as layer-4 escape hatch, not used in M4. The triangulation paid for itself in commit 5 — without macmon's 95% busy at B=8192, we'd have shipped batch-sweep numbers that systematically underreported real pipelined throughput by ~15%.
- **Decision (recorded):** `torch.profiler` on MPS torch 2.11 is **CPU-dispatch-only**. `ProfilerActivity.MPS` is not in this build. The methodology doc says this explicitly; we use the profiler to find op order and CPU sync points, not GPU kernel times. Updates to layer-3 expectations live in `mps-methodology/results.md` §4.
- **Decision (recorded):** Verifier is **baseline-aware**, not absolute. `EXPECTED_BOUNDS_CHECK_ITEMS_PER_CALL = 8` — calibrated empirically (the brief predicted 4; the trace showed 8 because `aten::lt`/`aten::ge` itself emits a scalar-extract pair before the `.any().__bool__()` does). When the bounds-check fix lands, the constant drops to 0 and the verifier doubles as a regression detector for the future PR.
- **Decision (recorded):** Streaming-output policy — every long-running tool (macmon NDJSON, profiler trace, sweep run.py, steady-state) writes structured output to a file; a one-line digest script reads it. **No LLM streaming.** Pattern enforced across the M4 work and locked in for future experiments.
- **Decision (recorded):** Bench-bracket per-call ≠ steady-state pipelined per-call. `mps.synchronize()` brackets stall the dispatch queue, adding ~0.2 ms of pure sync overhead per call. **Use bench numbers for cross-cell comparisons** (the stall is a fixed cost so ratios are honest); **apply the correction factor** when projecting absolute pipelined throughput. Worked example in `mps-methodology/results.md` §5 with macmon as ground-truth calibration. Two independent measurements (commit 5: 0.686 cold, commit 7: 0.866 warm) bracket the regime.
- **Decision (recorded):** Scaling efficiency = `throughput_ratio / batch_ratio`. Replaces raw % gain in `analyze.py` because the sweep grid uses non-uniform 8x batch steps — a "30% gain" sounds like saturation but linear-scaling at 8x batch would be 700%. Efficiency normalizes correctly for any batch step ratio. 0.80 onset / 0.40 heavy-saturation thresholds are heuristic but consistent with macmon's saturation regime at B=8192.
- **Decision (recorded):** **M4 measures, doesn't fix.** Two real CPU-sync gotchas in M2 code (bounds check + perm migration) were *measured* and *flagged*, not patched. Rationale (user's call): the methodology is itself under test — leaving the inefficiency in proves the verifier catches it, and lets a future agent independently rediscover and fix. Both gotchas have follow-up tickets in `LOG.md` decisions; the verifier's expected baseline drops to 0 once they land. Documents the discipline that will matter at M8 when measurement and optimization need to stay separate.
- **Decision (recorded):** Convention codified — every `experiments/<name>/` ends with a hand-written `intuition.md` (Observations / Hypotheses with confidence + evidence + verification plan / Open questions / Caveats), appended to auto-generated `results.md` by the dir's analyzer. Format invites investigation rather than asserting conclusions; distinguishes observation from inference; demands a verification plan for any unverified claim. In `CLAUDE.md` under Conventions; in project memory. Goal: capture conceptual learning per experiment without anchoring future agents to overconfident framings.
- **Deviation (recorded):** Plan said `ProfilerActivity.MPS` would let us read GPU kernel times. Empirically: not available on torch 2.11 — it's CPU-only. Methodology layer-3 reframed from "per-op GPU time breakdown" to "find CPU sync points + verify dispatch order." Honest scope change.
- **Deviation (recorded):** macmon CLI is `pipe` mode emitting NDJSON, not the `--samples N --duration T --output CSV` pattern the plan implied. Adapted (probe writes NDJSON; correlator reads NDJSON). The plan's "200ms × 30s" math survives, the format is just JSONL not CSV.
- **Deviation (recorded):** Discovered a *second* deferred CPU-sync gotcha (perm migration, env.py:70) that the plan didn't anticipate — only the bounds check was on the radar. Both now documented with verification plans.
- **Deviation (recorded):** `random_scrambles` top batch trimmed from 2_097_152 → 262_144 (apply_moves keeps full grid). Discovered during commit 7 setup: `random_scrambles` is depth-multiplied (×20) and CPU-bound on `move_seqs`, so 2M would have been a wall-clock outlier with no curve-shape benefit. `config.yaml` gained a `per_op_batch_sizes` block; `run.py` honors it via a single `.get(op, batch_sizes)` lookup.
- **Deviation (recorded):** Steady-state spot-check (`run_steady_state.py`) was added as a deliverable that wasn't explicitly in the plan — surfaced as essential during commit 5's macmon validation when bench-bracket per-call (0.694 ms) didn't match macmon's GPU-busy reading (95%). The correction factor became the methodology's most important nuance.
- **Deviation (recorded):** `analyze.py` heuristic upgraded mid-flight from `SATURATION_GAIN_THRESHOLD = 0.10` (raw % gain) to scaling efficiency. Surfaced in CHECKPOINT review when the auto-observations were too thin — the efficiency view actually tells the GPU-intuition story.
- **Deviation (recorded):** Intuition section + convention added at user request mid-flight (commit 7). Not in the original plan; became permanent project deliverable beyond M4. Worth flagging that the user's mid-stream conventions ("don't bias future agents") shaped the format more than the original plan did.
- **Deviation (recorded):** The first sweep attempt stalled inside a runaway subagent; restarted from main thread with explicit Bash `run_in_background`. Lesson: long-running synchronous work (multi-minute sweeps) is brittle inside subagents that don't manage waits cleanly. Future M8 sweeps should kick off via Bash background + Monitor, not delegate the wait to a subagent.
- Drive-by: `.claude/` (local Claude Code harness state — `scheduled_tasks.lock` etc.) added to `.gitignore`.
- Drive-by: `_format_throughput` reuse fix in `analyze.py` (originally referenced a non-existent helper; now uses `fmt_throughput` consistently).
- Drive-by: macmon worked-example AGREE result (`gpu_busy=0.952 gpu_power=3.94W expected=1.000`) embedded in `mps-methodology/results.md` §5 as the calibration ground truth.
**Commits:** 17269cd (open), c33e772, d0aafcd, 990afd2, 63242a2, 3a0b530, fd3bf76, 4f8e677 (close), ee56b82 (merge)

## 2026-05-01 — M3 visualization stack (2x2) ✅ done
**Goal:** Land the human-verifiable layer for the 2x2: ASCII renderer, SVG render components (single state / sequence grid / side-by-side compare), and a static HTML view of canonical cube states. Renderers consume *notations* (face-dict), not raw tensors. Acceptance: snapshot tests green; user can `open` the HTML view and visually confirm solved, single-move, and depth-20 scramble states render correctly.
**Milestone:** [plans/m3-viz.md](plans/m3-viz.md)
**Approach:** Single branch `m3-viz`. New module `src/rubik/viz/` with `colors.py` (palette as single source of truth), `ascii.py` (unfolded cross, face-letter cells), `svg.py` (3 functions: `render_svg_state`, `render_svg_sequence`, `render_svg_compare` — all return SVG fragment strings, embeddable in HTML). Tests mirror prior milestones: exact-string asserts for ASCII, `pytest-snapshot` for SVG (snapshots under `tests/viz/snapshots/`). Static gallery `visuals/cube_examples_2x2.html` with sections: solved, single-move (R/U/F), depth-8 canonical sequence (sexy²), depth-20 scramble side-by-side with its inverse.
**Outcome:**
- Acceptance gate green: `135 passed in 1.46s`, `ruff check` clean, `ruff format --check` clean. New viz tests: 13 (4 ASCII, 4 colors+palette, 5 SVG including custom-palette test).
- 3 source files added (`src/rubik/viz/colors.py`, `ascii.py` ~70 lines, `svg.py` ~280 lines), `viz/__init__.py` filled out, `rubik/__init__.py` `__all__` extended. 3 test files + `tests/viz/snapshots/` (4 SVG snapshots committed). 2 visualization generators + 2 generated HTMLs in `visuals/`. 1 codegen script moved to top-level `scripts/`.
- HTML view opened cleanly; visual self-check passed (solved uniform per face, after-R cycle correct, depth-20 inverse round-trips to solved, aesthetic matches the oracle rotations view).
- **Decision (recorded):** Renderers consume *face-dict* notation (`dict[str, list[int]]`), never raw tensors. SPEC §4 rule. Callers with a tensor do `render_ascii(state_to_dict(state, spec), spec)` — one line, no convenience-wrapper duplication. Adding a new state representation only requires writing a converter in `notation/`, not touching the renderers.
- **Decision (recorded):** SVG output is byte-stable across runs — single spaces between attributes, deterministic attribute order via f-strings, face iteration via `spec.faces`. Composition via `x_offset`/`y_offset` arithmetic (flat `<rect>` + `<text>` elements), not `<g transform="translate(...)">`. Equally deterministic, simpler diffs, no transform stack to reason about. `pytest-snapshot` byte-compares without flake.
- **Decision (recorded):** ASCII layout indent = `2 * spec.size` (= face-width-in-chars + inter-face separator). Generalizes cleanly: 2 spaces for 2x2, 6 for 3x3 at M5. Whole renderer body is `spec.size`-parametric — drops in for 3x3 with no new code.
- **Decision (recorded):** ASCII tests use the "record once, comment the geometry reasoning" pattern (the strict alternative — hand-deriving expected strings from `FACE_SLOTS` + the M1 R-cycle — was over-budget for the value). Each test has an inline comment explaining which corners moved where; future engine drift fails meaningfully against the recorded string.
- **Decision (recorded):** Naming convention codified after user feedback — *purpose first, size variant trails*. Files like `oracle_rotations_2x2.html`, scripts like `render_oracle_rotations_2x2.py`, tests like `test_cube_basic_fields_2x2`. CLAUDE.md updated. Renamed: `cubie_2x2_rotations.html` → `oracle_rotations_2x2.html`; `cube_preview_2x2.html` → `cube_examples_2x2.html`; generators to `render_<what>_<size>.py`. Reasoning: search-friendly grep `2x2` still finds everything, *and* identifiers read as purpose-then-variant rather than purpose-fragmented-by-variant.
- **Decision (recorded):** "Preview" dropped as the umbrella term — implies a before/after, but these renders *are* the production output (no real version downstream). Prefer "view" / "examples" / "gallery" depending on what's being shown. The umbrella directory stays `visuals/` (for visualizations).
- **Decision (recorded):** One renderer for cube-as-pixels — SVG via `viz.svg.render_svg_state`. The previous CSS-grid `<div class="sticker">` path in the oracle rotations generator was dropped; both visualization scripts now call `render_svg_state` for the cube renders. Page-level CSS (body bg, fonts, section layout) stays per-script. Single source of truth for "how a cube state becomes pixels"; tweaks propagate to all visualizations on regen.
- **Decision (recorded):** Palette is pluggable — `SPEEDCUBING_PALETTE` is *one* named palette of type `Palette = dict[int, str]`, all SVG renderers take an optional `palette` kwarg defaulting to `SPEEDCUBING_PALETTE`. New palettes drop into `viz/colors.py` alongside; callers swap without editing files. Naming reflects "it's a palette," not "the palette" — easier to add `MUTED_PALETTE`, `HIGH_CONTRAST_PALETTE`, etc. later.
- **Decision (recorded):** `scripts/` (top-level) is the home for pure-code dev tools that don't produce a visual artifact (e.g. `move_perm_from_oracle_2x2.py` derives the move-perm Python literal from the oracle and prints it for paste-into-env.py). Distinct from `visuals/scripts/` which is reserved for HTML/SVG generators. The M1-follow-up's "reserves `scripts/` from becoming a junk drawer" was about avoiding it as a viz dir; with `visuals/` owning that role, `scripts/` is now properly the dev-codegen home.
- **Decision (recorded):** `[tool.basedpyright] pythonVersion = "3.12"` added to `pyproject.toml` so PEP 695 `type Palette = ...` alias syntax stops getting flagged as "requires 3.12 or newer." We're on 3.12 (`.python-version`); the analyzer just needed to be told.
- **Deviation (recorded):** Plan listed 8 atomic commits; 11 landed. 8 from the initial implementation pass + 2 from the user-feedback pass (renames + renderer consolidation; palette parameterization) + 1 from the naming-convention codification (`_2x2` at end). Honest evolution.
- **Deviation (recorded):** Plan section 3 of HTML view was titled "Depth-10 canonical"; actual sequence is 8 moves (`R U R' U' R U R' U'`) so renamed to "Depth-8 canonical (sexy²)". Plan section 2 said "3-frame"; landed 4-frame (solved + R + U + F). Plan typos, corrected at implementation.
- **Deviation (recorded):** `render_svg_compare` signature uses `top_labels` / `bottom_labels` (per-frame lists) instead of the plan's singular `top_label` / `bottom_label` — the plan's HTML preview spec already implied per-frame ("Top labels: [...]"), so we went with the consistent shape.
- **Deviation (recorded):** Color legend in `render_oracle_rotations_2x2.py` was rewritten to use 6 small inline `<svg><rect/></svg>` swatches per face (one per face) instead of styled `<div class="sticker">` elements, since the dropped CSS-grid sticker path took the `.sticker` styles with it. Could be hoisted to a helper or re-use `render_svg_state` over a synthetic single-face dict; left simple for now. Backlog candidate.
- Drive-by: `[tool.basedpyright]` config added — fixed the spurious type-alias diagnostic (above).
- Drive-by: `src/rubik/cube/env.py` module docstring + `MOVE_PERM_2X2` snapshot comment updated to point at the new `scripts/move_perm_from_oracle_2x2.py` location.
**Commits:** 8d55969 (open), 434f206, 4c2769b, dec849b, d8a3a7b, 9415eb3, cd1f929, e8677b0, 966fa4e, dae822a (atop M2's `b4509b0` merge)

## 2026-05-01 — M2 fast tensor cube (2x2) ✅ done
**Goal:** Land the vectorized tensor cube for the 2x2. Batched `apply_moves`, `apply_move_sequence`, `is_solved`, `random_scrambles`, `valid_next_moves_mask` in `src/rubik/cube/env.py`. Move tables snapshotted from the M1 oracle (no runtime dep). Acceptance: all 2x2 identities; tensor ≡ oracle on 10k random sequences; inverse-of-scramble round-trip solves.
**Milestone:** [plans/m2-tensor-cube.md](plans/m2-tensor-cube.md)
**Approach:** Single branch `m2-tensor-cube`. State = flat `int8` sticker tensor `(spec.n_stickers,)` or batched `(B, n_stickers)`, exactly `CubeSpec.solved_state` shape. Move table = `(12, 24)` int64 permutation; apply via `torch.gather(states, -1, perm[move_idxs])`. Generate the snapshot offline via `visuals/scripts/generate_move_perm_2x2.py` (fingerprint renderer over the oracle); paste the literal into `env.py`; `_MOVE_PERM[spec.name]` dispatch keeps the code path 2x2/3x3 generic. Module-level functions, no `Cube` class. Device-agnostic, dtype-preserving. Tests mirror M1's identity suite + `EXPECTED_CW_POSITIONS` direction check, add 10k oracle-equivalence sweep, batched-apply, random-scrambles + same-face-prune, mask correctness, dtype/device preservation.
**Outcome:**
- Acceptance gate green: `122 passed in 1.41s`, `ruff check` clean, `ruff format --check` clean. New cube tests: 37 across all 15 plan sections (1 over plan: an explicit `test_random_scrambles_no_prune_allows_same_face` to cover the `prune_same_face=False` branch).
- 3 source files added (`src/rubik/cube/env.py` 208 lines, `tests/cube/test_env.py` 195 lines, `visuals/scripts/generate_move_perm_2x2.py` 112 lines), 2 modified (`src/rubik/cube/__init__.py` from empty to re-export hub, `src/rubik/__init__.py` extended `__all__`), plan + LOG/ROADMAP committed in the open commit.
- 10k oracle-equivalence sweep (depths 1–30) runs in well under 1 second on CPU — left at 10k as planned. Plenty of headroom to scale up if M5/M6 wants it; explicitly *not* a perf measurement.
- **Decision (recorded):** Tensor cube does **not** import the oracle at runtime. Move tables are static int64 permutations baked into `env.py` from a one-shot generator script (`visuals/scripts/generate_move_perm_2x2.py`). Drift is caught in CI by `test_move_perm_matches_oracle`, which re-derives the table inline and compares to the embedded constant. Implements SPEC.md line 230 ("*not* a runtime dependency on the oracle"). Generator lives under `visuals/` because it's a dev-time tool producing an out-of-tree artifact (the snapshot); fits the M1-follow-up convention of "artifact + generator both checked in."
- **Decision (recorded):** State representation = flat `int8` sticker tensor matching `CubeSpec.solved_state` shape `(spec.n_stickers,)` for single, `(B, spec.n_stickers)` for batched. No `Cube` class wrapper; module-level functions only. Tensors are the wire format (parity with M0/M1, parity with `notation/state.py` and `oracle/cubie.py`'s style). If a downstream phase wants method-style syntax we can wrap then; YAGNI now.
- **Decision (recorded):** `apply_moves` workhorse uses `torch.gather(states, -1, perm[move_idxs].to(states.device))`. Single int / 0-d tensor / 1-d tensor of move indices all flow through; broadcasts cleanly to `(S,)` or `(B, S)` states. Device-agnostic and dtype-preserving via gather semantics. No MPS-specific code at this milestone — that's M4's experiment loop.
- **Decision (recorded):** `_MOVE_PERM` dispatch dict keyed by `spec.name` (`"2x2"` for now, `"3x3"` drops in at M5 via `generate_move_perm_3x3.py` + paste). The "no 2x2/3x3 forks" rule from SPEC decision 1 is satisfied at the code level: same `apply_moves` body serves both sizes; only the data table differs. No size branching.
- **Decision (recorded):** `random_scrambles` defaults `prune_same_face=True` since DAVI / scramble generation typically wants it; the prune is implemented per-step via `torch.multinomial` on a per-row probability vector that zeros out same-face entries given the previous move. Tested both branches.
- **Deviation (recorded):** Plan listed 6 atomic commits; 7 landed because ruff lint/format fixups for the long-line `noqa: E501` markers on the 24-int snapshot rows + import-sort autofix were split into their own commit (`6536cef`) to keep the env.py and re-export commits clean. The `# fmt: off` / `# fmt: on` block + per-row noqa preserves single-line scannability of the snapshot, which would otherwise fragment under 88-column splitting and lose readability.
- **Deviation (recorded):** One extra test beyond the 15 in the plan (`test_random_scrambles_no_prune_allows_same_face`) — explicit coverage for the `prune_same_face=False` branch of `random_scrambles`. Without it that path was untested.
- Drive-by: `is_solved` returns a 0-d bool tensor for single-state input and `(B,)` for batched; tests use `bool(...)` / `.item()` to scalarize where needed. Same shape semantics as PyTorch's `.all(dim=-1)` reduction.
**Commits:** f379ea1, 5de9312, 8a2b198, 292b419, 6536cef, d60935d, 4b23a3f (atop opening commit 88dbf7b)

## 2026-05-01 — M1 follow-up: R direction fix + 2x2/3x3 naming + visuals/ ✅ done
**Goal:** Fix the R move (CW cycle was reversed — physically R'; user spotted via visual preview). Add direction-sensitive regression tests. Codify the **cube/cubie + `_2x2`/`_3x3` naming convention** in `CLAUDE.md` (search-friendly: `grep "2x2"` finds all 2x2-specific identifiers; `cube`/`cubie` stay generic). Reorganize HTML visualization artifacts into `visuals/` at the repo root with a `generate_<thing>.py` script convention.
**Milestone:** drive-by (M1 follow-up)
**Approach:** Single branch `cubie-r-direction-fix`. (1) Move files: `scripts/` → `visuals/`, with generators under `visuals/scripts/generate_<thing>_2x2_<thing>.py`. (2) Fix `_CW_MOVES["R"]` in `oracle/cubie.py`: invert the cycle (`URF → URB → DRB → DRF`) with inverse orientation deltas. (3) Add `test_cw_direction` parametrized over all 6 faces with hand-derived expected `state.positions`. (4) Apply naming convention to new identifiers (`generate_cubie_2x2_rotations.py`, `cubie_2x2_rotations.html`); leave `CUBE_2X2` as-is (already correct). (5) Add the `_2x2`/`_3x3` convention to `CLAUDE.md` so future code follows it. (6) Regenerate HTML, visually verify R now goes the right way.
**Outcome:**
- All gates green: `85 passed`, `ruff check` clean, `ruff format --check` clean. 6 new directional tests added (`test_cw_direction`, parametrized per face).
- File reorganization: `scripts/preview_rotations.py` → `visuals/scripts/generate_cubie_2x2_rotations.py`; `scripts/cubie_rotations.html` → `visuals/cubie_2x2_rotations.html`; `scripts/` removed.
- `_CW_MOVES["R"]` inverted: was `2→6→7→3→2` (physically R'), now `2→3→7→6→2` (physically R CW per right-hand-side viewing geometry). Orientation deltas inverse-flipped to match; new pattern `(1, 2, 1, 2)` around the new cycle.
- **Decision (recorded):** Cube/cubie + `_2x2`/`_3x3` naming convention — `cube` and `cubie` are generic (apply to both sizes); size-specific identifiers carry `_2x2` or `_3x3`. Goal: `grep "2x2"` finds every 2x2-specific identifier (constant, file, test function, etc.). Verbose but unambiguous and search-friendly; the alternative shorter `2`/`3` suffix collides with version numbers and other unrelated numerics. `CUBE_2X2` was already on this convention — kept as-is. Documented in `CLAUDE.md`. (User flip-flop history: briefly tried `CUBE2`/`cubie2` form before reverting to `_2x2` for searchability.)
- **Decision (recorded):** R-direction bug originated in my M1 plan — I prescribed the wrong cycle (`URF → DRF → DRB → URB`) for R CW. The brute-force search at implementation time produced consistent orientation deltas around that wrong cycle, and the algebraic identities (M⁴=I, Sune⁶=I, sexy⁶=I) are mirror-symmetric under R↔R' so they didn't catch it. The visual preview surfaced it within minutes — this validates the static-HTML preview pattern as a correctness check for cube-geometry code.
- **Decision (recorded):** Direction-sensitive testing — algebraic identities don't catch R↔R' inversion. The new `EXPECTED_CW_POSITIONS` table (6 hand-derived position permutations) + `test_cw_direction` is the only test in the suite that catches direction errors. M2's tensor cube must satisfy this same table.
- **Decision (recorded):** `visuals/` folder for human-verifiable HTML/SVG artifacts (out-of-tree, dev-time). Distinct from `src/rubik/viz/` (production renderers, M3+). Generators co-located in `visuals/scripts/generate_<thing>.py`. Both artifact and generator checked in. Reserves `scripts/` from becoming a junk drawer.
**Commits:** dc31a4a, 2b92ffa

## 2026-05-01 — M1 cubie oracle ✅ done
**Goal:** Hand-rolled cubie oracle for the 2x2 cube — corner state as `(positions, orientations)`, moves applied as physical rotations, sticker rendering. Pins within-face geometric ordering. Acceptance: M⁴=I, (RUR'U')⁶=I, Sune⁶=I, M'=M³, color-multiset preservation, 1000-step random walk without divergence.
**Milestone:** [plans/m1-cubie-oracle.md](plans/m1-cubie-oracle.md)
**Approach:** Frozen `CornerState` dataclass holding two 8-tuples. Bit-packed slot numbering `(U/D, L/R, F/B)`. U/D-axis orientation reference. Hand-write 6 CW move tables `dict[face, tuple[(slot_before, slot_after, ori_delta)*4]]`; derive CCW as CW³. `cubie_to_tensor(state, spec) -> torch.Tensor[24]` lives in `oracle/cubie.py` (cubie struct is internal, not a notation). Single test file `tests/oracle/test_cubie.py`. No new deps. Branch: `m1-cubie-oracle`.
**Outcome:**
- Acceptance gate green: `79 passed`, `ruff check` clean, `ruff format --check` clean. Oracle tests: 37 (10 categories, several parameterized over moves/faces).
- 4 source files added (`src/rubik/oracle/cubie.py` 207 lines, `src/rubik/oracle/__init__.py`, `tests/oracle/__init__.py`, `tests/oracle/test_cubie.py` 97 lines), 1 modified (`LOG.md`), plan committed to `plans/m1-cubie-oracle.md`.
- **Decision (recorded):** Within-face sticker layout pinned per the plan's `FACE_SLOTS` table — row-major reading order viewed from outside, with per-face "up" axes (U toward B, D toward F, side faces toward U). All downstream consumers (M2 tensor cube, M3 renderer) inherit this.
- **Decision (recorded):** Slot numbering bit-packed `(U/D, L/R, F/B)` — slot 0=ULF..7=DRB. Internal-only; not exposed in any user-facing API. Pays off in mask-based U-row vs D-row logic.
- **Decision (recorded):** CCW moves derived as `CW³` (composed from CW), not hand-written. The `M' = M³` test thereby reduces to "CW table is internally consistent" — fine because CW correctness is also covered by M⁴=I, (RUR'U')⁶=I, and Sune⁶=I.
- **Deviation (recorded):** The plan's `SLOT_FACETS` table prescribed `(UD, FB, LR)` ordering uniformly across all 8 slots. That labeling does not satisfy the algebraic identities under any consistent orientation-delta assignment — verified by hand-derivation, then by brute-force search over (SLOT_FACETS labeling × side-face orientation patterns). Final labeling is per-slot specific (4 of 8 slots differ from the plan): rotation geometry forces it, equivalent to standard speedcubing convention up to choice of "first side facet" at each slot. Captured in code comments in `oracle/cubie.py`.
- **Decision (recorded):** Side-face moves use `(1, 2, 1, 2)` orientation-delta pattern uniformly around each cycle (not a mix of patterns across L/F/R/B). Cleaner than the prompt's initial `(2, 1, 2, 1)` for R/B — both are mathematically valid but the uniform pattern was chosen for symmetry with the SLOT_FACETS labeling.
- **Decision (recorded):** `cubie_to_tensor` lives in `oracle/cubie.py`, not in `notation/`. Cubie struct is the oracle's internal representation; not promoted to a user-facing notation. `tensor_to_cubie` deliberately not implemented (sticker→cubie inversion non-trivial; M1 acceptance doesn't need it).
- **Decision (recorded):** `apply_move` raises `ValueError` on out-of-range move index — boundary validation, even though internal callers won't hit it.
- Drive-by: module-import asserts on `_CW_MOVES` (cycle is permutation, deltas sum to 0 mod 3) — cheap typo-catcher, runs once at import.
**Commits:** 5d86d55

## 2026-05-01 — M0 skeleton ✅ done
**Goal:** Land the package skeleton, `CubeSpec` abstraction, and notation hub stubs (move + state converters with round-trip tests). Acceptance: `uv run pytest` green, `uv run ruff check` clean, `CubeSpec(CUBE_2X2)` round-trips through every notation converter.
**Milestone:** [plans/m0-skeleton.md](plans/m0-skeleton.md)
**Approach:** Create the 7 package subdirs as `__init__.py` placeholders. Implement `cube/spec.py` (CubeSpec dataclass + `CUBE_2X2`), `notation/moves.py` (CubeSpec-independent QTM converters), `notation/state.py` (CubeSpec-parameterized tensor↔face-dict). Add ruff + pytest config to `pyproject.toml`. Tests under `tests/cube/` and `tests/notation/`. Branch: `m0-skeleton`.
**Outcome:**
- Acceptance gate green: `42 passed`, `ruff check` clean, `ruff format --check` clean.
- 17 source files added (7 skeleton `__init__.py`, 3 content modules, 5 test files + 2 empty test `__init__.py`), 2 modified (`pyproject.toml`, `src/rubik/__init__.py`), plan committed to `plans/m0-skeleton.md`.
- **Decision (recorded):** Face order locked at `("U","L","F","R","B","D")` — inherited from archived draft (lines 50–55) so the future encoding doc / 3x3 extension stays consistent. Move index mapping locked at `face_idx * 2 + direction` (CW=0, CCW=1) so same-face pruning is `move >> 1`.
- **Decision (recorded):** Within-face geometric ordering deliberately NOT pinned at M0. M0 commits only to face-block ordering and contiguity; M1's cubie oracle pins the within-face mapping when it generates physical-rotation move tables.
- **Decision (recorded):** `CubeSpec` kept lean — no `move_table`, no `face_to_color_map`, no corner/edge bookkeeping. Fields earn their way in by being needed downstream (move_table at M2, edge bookkeeping at M5 if oracle architecture demands).
- **Decision (recorded):** `solved_state` is a `@property` returning a fresh tensor each call (no caching). Avoids aliasing surprises if a caller mutates; cost is negligible (24-element int8 tensor). Locked in via a `test_solved_state_returns_fresh_tensor`.
- Drive-by: `state_to_dict` validation extended to also cover bad-shape inputs (plan only specified bad-input cases for `dict_to_state`).
**Commits:** 164e0da

## 2026-05-01 — Bootstrap project ✅ done
**Goal:** Initialize the repo, package skeleton, foundational docs, and first commit.
**Milestone:** drive-by (pre-M0)
**Approach:** `git init`, `uv init --lib`, add deps, write SPEC / ROADMAP / CLAUDE / LOG, archive the original draft spec, commit.
**Outcome:**
- `git init -b main`; default branch `main`.
- `.gitignore` covering Python, macOS, IDE, env, and project-specific (model checkpoints, training logs, `experiments/*/runs/`, `experiments/*/results/`).
- `uv init --lib --no-readme --python 3.12` produced `src/rubik/` package with `py.typed`. Trimmed auto-generated `authors` and updated `description` in `pyproject.toml`.
- Deps: runtime `torch`, `numpy`; dev `pytest`, `ruff`, `pytest-snapshot`. No pycuber (3x3-only, unmaintained); no tensorboard / wandb / matplotlib (deferred).
- Authored `CLAUDE.md` (opts into cc-process via `@~/.claude/cc-process.md`, codifies tooling + key conventions), `SPEC.md` (project goal, 8 key decisions with rationale, M0–M9 acceptance criteria, package layout, references), `ROADMAP.md` (cc-process format), `LOG.md` (this file).
- Original draft moved to `plans/archive/llm-draft-spec.md` — retained for technical detail (sticker indexing, network architecture, hyperparam starting points), no longer authoritative.
- **Decision (recorded):** 2x2 first, parameterized via `CubeSpec` — single code path, no 2x2/3x3 forks. Two-witness correctness (cubie oracle + tensor); pycuber dropped. QTM only at first. Notation hub as a first-class module. Visualization is its own milestone (M3) with HTML/SVG preferred over matplotlib. "Make it work, then make it fast" — perf bar moves to M4. Experiment-loop pattern established at M4, reused at M8.
**Commits:** 9522ce0
