# DAVI 3x3 — per-run results

_One section per run: train loss, eval macro-MAE trajectory, per-depth MAE at start / middle / end, and greedy-solve trajectory. The side-by-side cross-run views (overlaid charts, histograms, wavefront heatmap) live in `error_trajectories.html`._

_(No runs registered yet. The first cycle lands in P2b — see `plans/m8-3x3-davi.md`. Append to `RUNS` in this script when a new run lands.)_
