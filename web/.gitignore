node_modules
dist
dist-ssr
*.local

# Staged model artifacts (sourced from experiments/davi-3x3/runs/...).
# The two web-demo weights ship via Git LFS so static deploys (Vercel)
# have them at build time; anything else staged here stays out of the repo.
public/models/*
!public/models/net_final.onnx
!public/models/net_final.onnx.data
playwright-report
test-results
.playwright
.vite

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
